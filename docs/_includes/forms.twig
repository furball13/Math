{% macro input(name, label, value, type, size) %}
  <label for="{{ name }}">{{ label ?: name|capitalize }}: </label>
  <input type="{{ type|default('text') }}" name="{{ name }}" value="{{ value|e }}" size="{{ size|default(20) }}" />
{% endmacro %}
